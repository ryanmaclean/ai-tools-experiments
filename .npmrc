# .npmrc configuration
# Skip optional dependencies to avoid canvas build issues in Docker
optional=false

# Skip unnecessary scripts
ignore-scripts=false

# Use production dependencies only in Docker builds
# production=true

# Force use of package resolutions to avoid deprecated package warnings
legacy-peer-deps=true
force=true

# Set environment variables differently, not through npm config
# Environment variables should be set in Dockerfile or docker-compose
